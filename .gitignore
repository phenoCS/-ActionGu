# Runtime data generated by the app (auto-created on first run)
data.json

# Logs (launcher writes to %TEMP%, but ignore any stray local logs)
*.log

# Python caches
__pycache__/
*.pyc

# Build artifacts (only used when building the optional standalone exe)
build/
dist/
*.spec
